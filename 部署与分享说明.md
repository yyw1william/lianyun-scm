# 链运 SCM · 如何分享给别人

## 现状
原型目前是**本地文件**，没有网址：
- 单页外壳：`mockup/APP.html`（一个文件包含全部 9 页 + 数据闭环，自包含，可单独分享）
- 9 个单页：`mockup/P1~P9-*.html`

## 方式一：直接把文件发过去（最快，推荐先这样）
1. 把 `mockup/APP.html` 这一个文件通过微信 / 钉钉 / 邮件 / 网盘发给对方；
2. 对方下载后，**双击**用 Chrome 打开即可，全功能可用（无需联网，字体离线会回退系统字体）。
> 适合：面试官 / 导师预览。缺点：没有网址，不能直接点链接看。

## 方式二：GitHub Pages（免费、有 https 链接，推荐）
1. 注册/登录 [github.com](https://github.com)；
2. 新建仓库（如 `lianyun-scm`），把 **`mockup/` 文件夹里所有文件**上传（仓库根目录放 `APP.html` 最方便）；
3. 仓库 Settings → Pages → Source 选 `main` 分支 / root；
4. 约 1 分钟后得到链接：`https://你的用户名.github.io/lianyun-scm/APP.html`。
> 优点：任何人都能点链接打开，且数据在各自浏览器本地保存，互不干扰。
> 备注：APP.html 已内嵌全部页面，部署后 iframe 与页面同源，数据闭环依然成立（已验证）。

## 方式三：Netlify / Vercel 拖拽部署（不用命令行，最快出链接）
1. 打开 [app.netlify.com/drop](https://app.netlify.com/drop)；
2. 把 `mockup/` 文件夹直接拖进去；
3. 立即得到一个 `https://xxxx.netlify.app` 链接，把链接发给别人即可。
> Vercel 同样支持拖拽：vercel.com → New Project → 导入文件夹。

## 方式四：国内网络环境（如果对方打不开 GitHub/Netlify）
- 腾讯云 COS / 阿里云 OSS：建一个公开读的存储桶，把 `mockup/` 传上去，开启静态网站托管，得到 CDN 链接；
- 或者用语雀 / 飞书文档上传附件，让对方下载后双击打开（等同方式一）。

## 常见问题
- 对方打开后数据是空的/是演示数据？—— 正常，数据存在各自浏览器 localStorage，首次打开是内置演示数据；点外壳右上角「↺ 重置演示数据」可恢复。
- 文件太大？—— APP.html 约 320KB，内嵌了 9 页源码，属正常。

---

## 永久链接部署（放简历用，2026-08-20 更新）

### 为什么必须有账号这一步
所有真正「永久 + 公开 + 专业」的托管（GitHub / Netlify / Cloudflare / 腾讯云）都要求一个邮箱账号——
这是平台反滥用与身份验证的硬性要求，我无法替你注册（涉及你的身份验证）。我能做的是把其余步骤全部自动化。

### 方案 A：GitHub Pages（推荐，永久、可更新、面试官认可）
我已备好一键包，你的步骤只需：
1. 注册/登录 GitHub（github.com，免费，用邮箱即可）；
2. 在终端执行 `brew install gh` 后 `gh auth login` 登录（或告诉我你的 GitHub 用户名 + 一个 Personal Access Token，我帮你全程执行）；
3. 双击 **`部署到GitHubPages.command`** → 自动建仓库、推代码、开启 Pages。

结果：永久链接 `https://你的用户名.github.io/lianyun-scm/` + 配套二维码（我生成）。
以后改原型：双击 **`更新原型并推送.command`** 即可更新，链接不变。

### 方案 B：Netlify 拖拽（30 秒，只需邮箱）
1. 浏览器打开 [app.netlify.com/drop](https://app.netlify.com/drop)；
2. 把 **`mockup/` 文件夹**（或 `docs/` 里的 index.html）拖进去；
3. 部署完成后页面会提示「Claim your project」→ 输入邮箱注册即可公开；
4. 得到永久链接 `https://xxx.netlify.app`，发给我，我生成配套二维码。

### 说明
- 已准备好：`docs/index.html`（站点根文件）、`部署到GitHubPages.command`、`更新原型并推送.command`、本地 git 仓库（已提交）。
- 在永久链接确定前，临时 24h 链接仍可先用（双击「重新生成分享链接.command」）。
