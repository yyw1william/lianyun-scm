# 链运 SCM · 如何分享给别人

## 🌐 两个永久链接（均已上线）

| 入口 | 链接 | 说明 |
|---|---|---|
| 主站 | https://lianyun-scm.netlify.app | Netlify，主用 |
| 备用 | https://yyw1william.github.io/lianyun-scm/ | GitHub Pages，源码在 https://github.com/yyw1william/lianyun-scm |

更新原型时：改完 `mockup/APP.html` → 同步到 `docs/index.html` → `git push`（Pages 自动重建）+ Netlify 重新 deploy，两个链接都不变。

## ✅ 永久链接（放简历用，已部署完成）

- **网址**：https://lianyun-scm.netlify.app
- **二维码**：`链运SCM-分享二维码.png`（本目录下，已校验可解码，指向上述永久链接）
- **平台**：Netlify（免费、https、永久有效、任何人点开即用）
- **管理后台**：https://app.netlify.com/projects/lianyun-scm（账号：William Wang / 18815224378@163.com）
- **内容**：单页外壳 `docs/index.html`（内含全部 9 页 + AI 录入/分析 + 移动端适配 + 数据闭环）

> 链接**永久有效、不依赖临时网盘**，扫码 / 点击均可直接打开，无需登录。
> 注意：Netlify 新建站点默认开启了「访问保护（Login Redirect）」，已通过 API 关闭（`sso_login=false`），
> 请勿在后台误开「Require login」，否则链接会变回登录页。

### 以后更新原型怎么办
用部署脚本（已配好 netlify-cli，位于 `/private/tmp/nfcli`）：
```bash
# 1) 把最新 docs/index.html 复制到部署目录
cp "/Users/y/Documents/ChatGPT/供应链产品/docs/index.html" /private/tmp/site_deploy/index.html
# 2) 在部署目录内发布
cd /private/tmp/site_deploy && /private/tmp/nfcli/node_modules/.bin/netlify deploy --prod --site c860a538-90fc-48b8-9b63-37805e4d99d7
```
链接保持不变，更新后约 1 分钟生效。

---

## 其他分享方式（备用）

### 方式一：直接把文件发过去
把 `mockup/APP.html` 这一个文件通过微信 / 钉钉 / 邮件 / 网盘发给对方；对方下载后**双击**用 Chrome 打开即可，全功能可用。

### 方式二：临时链接（24h，兜底用）
双击「重新生成分享链接.command」，或：
```bash
cd mockup && curl -F "reqtype=fileupload" -F "time=24h" -F "fileToUpload=@APP.html" https://litterbox.catbox.moe/resources/internals/api.php
```

### 方式三：GitHub Pages（可选，作为第二份永久链接）
- `部署到GitHubPages.command` 已备好，登录 GitHub 后双击即可，得到 `https://你的用户名.github.io/lianyun-scm/`。

---

## 常见问题
- 对方打开后数据是空的/是演示数据？—— 正常，数据存在各自浏览器 localStorage，首次打开是内置演示数据；点外壳右上角「↺ 重置演示数据」可恢复。
- 文件太大？—— APP.html 约 320KB，内嵌了 9 页源码，属正常。
